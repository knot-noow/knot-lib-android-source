<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sample.knot.cesar.org.br.drinkingfountain">

    <application
        android:name=".application.DrinkApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

	    <activity android:name=".ui.MapActivity"></activity>

        <!-- Service used to sync all data -->
        <service
            android:name=".service.SyncService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="sample.knot.cesar.org.br.drinkingfountain.ACTION_SYNC_DATA" />
            </intent-filter>
            <intent-filter>
                <action android:name="sample.knot.cesar.org.br.drinkingfountain.ACTION_STOP_SYNC_DATA" />
            </intent-filter>
            <intent-filter>
                <action android:name="sample.knot.cesar.org.br.drinkingfountain.ACTION_SYNC_STATUS" />
            </intent-filter>
        </service>
        <!-- End -->

    </application>

</manifest>
